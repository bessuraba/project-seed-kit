# GTM plan — <project name> (living)

**Living document — decisions and roadmap, edited in place as decisions are made.** Applies the
frameworks in `../methodology/` to this project specifically. Not a static plan — update this file
directly when a decision changes, don't fork a new dated copy (that's what `../research/` is for).

## Current synthesis

(Key decisions made so far, each citing the research/synthesis that grounds it. Example shape:
discovery status, who the ICP is and why, positioning, what's being built and why that scope, what
the actual go/measure/decide sequence is.)

## Open items

(A single prioritized checklist — `[ ]` open, `[x]` done with a one-line note on how/when. Split
into stages if useful — e.g. blockers vs. backlog vs. later — once the list gets long enough that
flat priority stops being obvious.)
