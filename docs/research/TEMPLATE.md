# Research session — <topic> (YYYY-MM-DD)

**Raw research artifact — frozen as of this date, not edited after.** One file per research
session. A correction to an earlier file is a NEW dated file, never an edit to this one.

State up front: what prompted this session, what question it's answering, what method was used
(web search, live API/site checks, interviews, etc.).

## Findings

The actual findings, with sources. Prefer live-verified facts over assumptions — if something is
checked against a real site/API/document, say so and link it. If something is inferred or
uncertain, say that explicitly too.

## What this means / what changed

If this session's findings should update the project's current understanding, name which
`../synthesis/*.md` or `../plans/*.md` file to update — but do that update in that file, not here.
This file stays exactly as written once the session ends.

---

Delete this template text and rename the file to `YYYY-MM-DD-topic.md` when starting a real
research session.
