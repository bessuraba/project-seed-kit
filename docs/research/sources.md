# Source channels — running log

**Exception to the dated-immutable rule** — this file is updated in place, not dated. A log of
where useful information has actually been found (and where it hasn't), so future research
sessions don't re-discover the same dead ends.

## Worked well

(Nothing yet — add real channels as they prove useful: specific subreddits, forums, comparison
sites, APIs, etc. Name the channel and what it was useful for.)

## Didn't work / dead ends

(Add channels that looked promising but produced nothing, so nobody re-tries them from scratch.)
